import { fonts } from "../../../assets/fonts";
const { StyleSheet } = require("react-native");
const styles = StyleSheet.create({
    font:{
        fontFamily:fonts.CircularSTD.regular
    }
})
export default styles;